
// Import the component from the original location
// This is a placeholder for the ImageViewerDialog component
import ImageViewerDialogFromOriginalLocation from '@/components/project-layouts/maintenance/ImageViewerDialog';

// Re-export the component
const ImageViewerDialog = ImageViewerDialogFromOriginalLocation;

export default ImageViewerDialog;

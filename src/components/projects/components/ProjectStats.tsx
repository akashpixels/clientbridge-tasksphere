
// Import the component from the original location
// This is a placeholder for the ProjectStats component
import ProjectStatsFromOriginalLocation from '@/components/project-layouts/maintenance/ProjectStats';

// Re-export the component
const ProjectStats = ProjectStatsFromOriginalLocation;

export default ProjectStats;
